
  # Mobile Restaurant Menu UI

  This is a code  for Mobile Restaurant Menu UI. 

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  